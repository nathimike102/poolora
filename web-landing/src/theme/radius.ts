/**
 * Border-radius tokens. The base scale maps to Tailwind's `rounded-*`
 * utilities; `device` is the custom radius used by the Hero phone mockup.
 */
export const radius = {
  lg: '0.5rem',
  xl: '0.75rem',
  '2xl': '1rem',
  '3xl': '1.5rem',
  full: '9999px',
  device: '3.2rem',
} as const;
