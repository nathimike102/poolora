/**
 * Typography tokens. The app renders with the Plus Jakarta Sans stack defined
 * here and in `src/styles/theme.css`.
 */
export const fontFamily = {
  sans: "'Plus Jakarta Sans', sans-serif",
} as const;

export const fontWeight = {
  normal: 400,
  medium: 500,
  semibold: 600,
  bold: 700,
  black: 900,
} as const;
