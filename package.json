{
  "name": "poolora-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "web-landing"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "lint": "npm run lint --workspace backend",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace frontend && npm run typecheck --workspace web-landing",
    "test": "npm run test --workspace backend && npm run test --workspace frontend"
  },
  "version": "1.0.0",
  "devDependencies": {
    "async-limiter": "^2.0.0"
  }
}
