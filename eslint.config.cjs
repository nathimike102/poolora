module.exports = {
  // Minimal root config to satisfy ESLint v9+ requirement and allow legacy .eslintrc.* usage.
  // This keeps the repo using existing .eslintrc.cjs files while silencing the missing-file error.
  root: true,
};
