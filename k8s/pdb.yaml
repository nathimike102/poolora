apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: poolora-backend-pdb
  namespace: poolora
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: poolora-backend

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mongodb-pdb
  namespace: poolora
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: mongodb

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: redis-pdb
  namespace: poolora
spec:
  # Single instance: allow voluntary evictions so node drains are not blocked.
  maxUnavailable: 1
  selector:
    matchLabels:
      app: redis