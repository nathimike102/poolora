apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-storage
  namespace: poolora
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: standard